后端语言，到底是python还是go？需要确定

中间用到了消息队列，用到了ETCD，也用到了缓存，是否可以减少组件使用，减少暴露面，使用ETCD，然后使用list-watch的方式实现消息队列的功能

调度模块，应该使用现在的cron这种机制，还是用etcd的lease机制来实现，或者通过实现一个控制器来实现呢？

调度模块，并发控制逻辑中，用到了令牌、redis、LUA脚本、队列，是否有更简单的设计，或者更高效的设计，是否用一个控制器用声明式的方式？

任务优先级队列的视线，用到了redis、RabbitMQ，是否可以用更简单的设计实现，使用ETCD的list-watch机制能否实现？

数据处理模块，给出了交互流程图，但是是否还可以给出更详细的设计方案？

数据存储模块，画了图，能否细化出来都包含哪些功能，哪些具体模块儿，具体接口？

监控告警模块，能否把一些对应的组件版本选型确定下来，比如监控用Prometheus，告警用Alertmanager，日志用ELK，指标采集用node-exporter等等。

有一些图是mmd图，这些图是之前的，通读全文，确认一下这些是否正确，是否能够反映出整体设计的意图。并考虑将mmd的图转换成更通用的planuml的UML图

插件系统，给出了示意图，但是插件应该怎么设计还没有细化，插件的生命周期，插件的接口规范，插件的管理方式等等

数据库设计，检查是否完善，检查是否还需要补充，是否考虑了数据的分片、备份、恢复等策略？

第七章，核心场景流程图比较少，需要继续补充，将核心场景的流程图补充完整

第八章核心类设计也比较少，需要继续补充完善

任务管理模块，比较简单，是否可以补充更多的设计细节，作为模块设计的方案

第十一章性能优化，应该补充性能、可靠性、安全性等方面的设计方案

运维与监控部分，是否可以补充更多的细节设计方案

整体系统设计，是否可以用k8s的Operator模式来实现？





1、前后端分离的微服务架构：web、api、服务注册、控制、执行、消息队列、缓存、数据库
2、声明式：基于CRD的任务定义，调度，任务状态通过控制器来驱动，替换掉消息队列、缓存等组件
（在1和2中，选2）

3、使用argo、dify等工作流组件开发